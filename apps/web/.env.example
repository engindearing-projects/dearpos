# Postgres connection
DATABASE_URL="postgresql://dearpos:dearpos@localhost:5432/dearpos?schema=public"

# Stripe — Terminal (in-store card processing)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Stripe — Hosted SaaS subscription (create $29/mo recurring price in Stripe Dashboard)
STRIPE_PRICE_HOSTED="price_..."

# Email — Resend for welcome emails (get free key at resend.com; omit to log-only)
RESEND_API_KEY="re_..."

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
